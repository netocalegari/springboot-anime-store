server:
  port: 8081
  error:
    include-stacktrace: on_param

spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/anime_store
    username: root
    password: root
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
